#include <iostream>

using namespace std;

int main()
{
    cin >> n >> m;
    char map[n][m];
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> map[i][j];

    //╩С го аб ©Л (x,y)
    int dir[4][2] = {{0, 1}, {0, -1}, {-1, 0}, {1, 0}};

    if
}